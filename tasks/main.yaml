---
- name: Install prerequisites
  apt:
    name:
      - apt-transport-https
      - ca-certificates
      - curl
      - gnupg
      - lsb-release
    update_cache: yes

- name: Add Docker’s official GPG key
  apt_key:
    url: https://download.docker.com/linux/ubuntu/gpg
    state: present

- name: Add Docker repository
  apt_repository:
    repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_lsb.codename }} stable"
    state: present
    filename: docker

- name: Install Docker Engine
  apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    update_cache: yes
  notify: Restart docker

- name: Ensure docker group exists
  group:
    name: docker
    state: present

- name: Add user to docker group
  user:
    name: "{{ docker_user | default(ansible_user_id) }}"
    groups: docker
    append: yes
  when: docker_install_mode != 'rootless'

- name: Install rootless dependencies
  apt:
    name:
      - uidmap
      - dbus-user-session
    state: present
  when: docker_install_mode == 'rootless'

- name: Install Docker Rootless (script)
  become: false
  shell: |
    curl -fsSL https://get.docker.com/rootless | sh
  environment:
    DOCKER_CHANNEL: stable
  args:
    creates: "/home/{{ docker_user | default(ansible_user_id) }}/bin/dockerd-rootless.sh"
  when: docker_install_mode == 'rootless'

- name: Enable linger for rootless user (systemd user units allowed after logout)
  command: "loginctl enable-linger {{ docker_user | default(ansible_user_id) }}"
  become: true
  when: docker_install_mode == 'rootless'

- name: Enable and start docker.service (systemd, normal mode)
  systemd:
    name: docker
    enabled: true
    state: started
  when: docker_install_mode != 'rootless'

- name: Start rootless docker as user (systemd user unit)
  become: false
  shell: |
    XDG_RUNTIME_DIR=/run/user/$(id -u) systemctl --user enable docker
    XDG_RUNTIME_DIR=/run/user/$(id -u) systemctl --user start docker
  args:
    executable: /bin/bash
  when: docker_install_mode == 'rootless'

- name: Print Docker info
  command: docker info
  register: docker_info
  ignore_errors: yes

- name: Show Docker info
  debug:
    var: docker_info.stdout
